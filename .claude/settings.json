{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$f\" in \"$CLAUDE_PROJECT_DIR\"/.claude/*|\"$CLAUDE_PROJECT_DIR\"/CLAUDE*) ;; \"$CLAUDE_PROJECT_DIR\"/*) \"$CLAUDE_PROJECT_DIR\"/install.sh >/dev/null 2>&1 || true ;; esac",
            "timeout": 60,
            "statusMessage": "Installing bar drawer into omarchy-shell..."
          }
        ]
      }
    ]
  }
}
